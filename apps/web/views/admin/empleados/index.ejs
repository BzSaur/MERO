<!DOCTYPE html>
<html lang="es">
<head>
  <%- include('../../partials/head') %>
</head>

<body class="sidebar-collapsed">
  <%- include('../../partials/icons') %>

  <div class="wrapper">
    <%- include('../../partials/sidebar') %>

    <div class="main-content">
      <%- include('../../partials/navbar') %>

      <main class="content">
        <div class="page page--usuarios">
          <div class="container">

            <div class="page-header page-header--usuarios page-header--empleados-list">
              <div>
                <h1>Empleados</h1>
                <h2><p class="page-header__sub">
                  Lectura en tiempo real desde VITA (ERP) &mdash; <%= empleados.length %> activos
                </p></h2>
              </div>

              <form action="/admin/empleados/sync" method="POST">
                <button type="submit" class="btn btn--create">
                  <svg class="icon"><use href="#icon-refresh"/></svg>
                  Sincronizar con VITA
                </button>
              </form>
            </div>

            <%- include('../../partials/flash') %>

            <div class="card">
              <div class="card__header card__header--usuarios">
                <h3><span class="card__title">Listado de empleados</span></h3>
                <span class="badge badge--ok"><%= empleados.length %></span>
              </div>

              <% if (!empleados.length) { %>
                <div class="empty">
                  <p class="empty__text">
                    No hay empleados sincronizados. Presiona "Sincronizar con VITA" para importarlos.
                  </p>
                </div>
              <% } else { %>
                <div class="table-wrap table-wrap--clean">
                  <table class="table table--clean">
                    <thead>
                      <tr>
                        <th>ID VITA</th>
                        <th>Nombre</th>
                        <th class="td-hide-mobile">Puesto</th>
                        <th class="td-hide-mobile">Área (VITA)</th>
                        <th class="td-hide-mobile">Fecha Ingreso</th>
                        <th>QR</th>
                        <th>Acciones</th>
                      </tr>
                    </thead>

                    <tbody>
                      <% empleados.forEach(e => { %>
                        <tr>
                          <td class="td-mono"><%= e.idVita %></td>

                          <td>
                            <strong><%= e.nombre %> <%= e.apellidos || '' %></strong>
                          </td>

                          <td class="td-hide-mobile"><%= e.vita?.puesto || '—' %></td>
                          <td class="td-hide-mobile"><%= e.vita?.area || '—' %></td>

                          <td class="td-hide-mobile td-mono">
                            <%= e.vita?.fechaIngreso ? new Date(e.vita.fechaIngreso).toLocaleDateString('es-MX') : '—' %>
                          </td>

                          <td>
                            <a
                              href="/admin/empleados/<%= e.id %>/qr-download"
                              class="badge badge--ok"
                              title="<%= e.uuidQr %>"
                              download
                            >
                              <svg class="icon"><use href="#icon-qr"/></svg>
                              QR
                            </a>
                          </td>

                          <td>
                            <div class="row-actions">
                              <a href="/admin/empleados/<%= e.id %>" class="btn btn--ghost btn--sm">
                                <svg class="icon"><use href="#icon-eye"/></svg>
                                Ver
                              </a>
                            </div>
                          </td>
                        </tr>
                      <% }) %>
                    </tbody>
                  </table>
                </div>
              <% } %>
            </div>

          </div>
        </div>
      </main>
    </div>
  </div>

  <div id="sidebarOverlay"></div>
  <script src="/js/main.js"></script>
</body>
</html>