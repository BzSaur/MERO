services:
  db:
    image: postgres:16-alpine
    container_name: mero-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-mero}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-mero_secret}
      POSTGRES_DB: ${POSTGRES_DB:-mero_db}
      TZ: America/Mexico_City
    ports:
      - '${DB_PORT:-5433}:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data
      - backups:/backups
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${POSTGRES_USER:-mero} -d ${POSTGRES_DB:-mero_db}']
      interval: 10s
      timeout: 5s
      retries: 5

  api:
    build:
      context: ../../
      dockerfile: infra/docker/Dockerfile
      target: production
    container_name: mero-api
    restart: unless-stopped
    env_file:
      - ../../.env
    environment:
      TZ: America/Mexico_City
      DATABASE_URL: postgresql://${POSTGRES_USER:-mero}:${POSTGRES_PASSWORD:-mero_secret}@db:5432/${POSTGRES_DB:-mero_db}?schema=public
    ports:
      - '${API_PORT:-3002}:3000'
    networks:
      - default
      - vita_network
    depends_on:
      db:
        condition: service_healthy

  web:
    build:
      context: ../../
      dockerfile: infra/docker/Dockerfile.web
    container_name: mero-web
    restart: unless-stopped
    env_file:
      - ../../.env
    environment:
      TZ: America/Mexico_City
      API_URL: http://api:3000/api
    ports:
      - '${WEB_PORT:-4000}:4000'
    depends_on:
      - api

  nginx:
    image: nginx:alpine
    container_name: mero-nginx
    restart: unless-stopped
    ports:
      - '8080:80'
      - '8081:8080'
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    networks:
      - default
      - vita_network
    depends_on:
      - api
      - web

  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: mero-cloudflared
    restart: unless-stopped
    command: tunnel --no-autoupdate run
    environment:
      TUNNEL_TOKEN: ${CLOUDFLARE_TUNNEL_TOKEN}
    network_mode: host
    depends_on:
      - nginx

networks:
  default:
  vita_network:
    external: true
    name: erp_erp_network

volumes:
  pgdata:
  backups:
